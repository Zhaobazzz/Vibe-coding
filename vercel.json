{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd \"Vibe coding 前端/frontend\" && npm install",
  "buildCommand": "cd \"Vibe coding 前端/frontend\" && npm run build && rm -rf ../../dist && cp -R dist ../../dist",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
